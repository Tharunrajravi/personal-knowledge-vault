# Copy to .env.local for local development
# Vite reads VITE_* variables and injects them at build time

# Leave empty to use relative URLs (works with CloudFront in prod)
# Set to your ALB URL for direct local dev without docker-compose
VITE_API_URL=
